doctype html
html
  head
    meta(charset='UTF-8')
    meta(name='format-detection', content='telephone=no')
    meta(
    name='viewport'
    content='width=device-width,initial-scale=1.0,minimum-scale=1.0,maximum-scale=1.0,user-scalable=no'
    )
    title 注册
    link(rel='stylesheet', href='./css/register.css')
    script.
      (function (document, window, size) {
        var html = document.documentElement,
          resizeEvent = 'orientationchange' in window ? 'orientationchange' : 'resize',
          calcFontSize = function () {
            var deviceWidth = html.clientWidth > 1300 ? 1300 : html.clientWidth
            html.style.fontSize = 100 * (deviceWidth / size) + 'px'
          }
        if (!document.addEventListener) { return }
        window.addEventListener(resizeEvent, calcFontSize, false)
        document.addEventListener('DOMContentLoaded', calcFontSize, false)
      })(document, window, 750)
    script(src='http://code.jquery.com/jquery-1.12.4.min.js', defer)
    script(src='./js/register.js', defer)
  body
    div#continaer
      header
        img.logo(src='./images/logo.png')
      section
        a.area(href='javascript:;')
          span 国家/区域
          span.flex 中国
          i.iconfont &#xe66e;
        div.input-item.auth
          i.iconfont &#xe660;
          input(placeholder='请输入你的手机号码' type='tel' name='telephone' maxlength='20')
          button#getcode(type='button' disabled) 获取验证码
        div.input-item
          i.iconfont &#xe614;
          input(placeholder='请输入验证码', name='verification')
        div.input-item.password
          i.iconfont &#xe63d;
          input.pwd(type='password', placeholder='请输入密码', name='password' maxlength='20')
          i.iconfont.right.hided &#xe618;
        div.other-entry
          label
            input(type='checkbox', checked)
            | 注册即表示同意
            a(href='javascript:;') 《用户协议》
            i.checkbox-mask
        div.button-box
          a.button.submit(href='javascript:;') 注册
          a.register(href='javascript:;')
            span 已有账号？
            |直接登录
      footer
        a(href='javascript:;')
          i.iconfont &#xe692;
          span 联系客服
